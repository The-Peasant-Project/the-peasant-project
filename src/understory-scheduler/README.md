# understory-scheduler

Not started. Read `docs/11-build-specs/scheduler.md` and `AGENTS.md` first. First deliverable is `plan(state) -> Plan` as a pure function with the five acceptance tests from the spec, plus a simulator that replays historical Victorian price data.
